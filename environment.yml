name: dicom-pdf
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - -r requirements.txt
  - flake8
  - pytest
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pydicom
  - pillow
  - numpy
  - reportlab
  - pip
  - pip:
      - requests
